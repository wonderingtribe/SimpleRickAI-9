rootProject.name = "Ai-simple-rick"
include ':app'
gradle.startParameter.useEmptySettings = true

if (!gradle.gradleHomeDir?.exists()) {
    println "⚠ Gradle wrapper not executable — falling back to system Gradle"
}
